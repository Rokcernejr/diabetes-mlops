{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "diabetes-mlops.serviceAccountName" . }}
  labels:
    {{- include "diabetes-mlops.labels" . | nindent 4 }}
{{- end }}
