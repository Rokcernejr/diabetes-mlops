# One-off operational artifacts

Debugging/setup leftovers kept for reference (Route53 record payloads, CORS
annotation experiments, a browser-based API test page). Nothing here is wired
into the build, deploy, or test pipelines.
